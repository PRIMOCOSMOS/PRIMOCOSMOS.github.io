---
version: 1
slug: "src-components-workstation-workstation-tsx"
primary_target: "src/components/workstation/Workstation.tsx"
related_targets: ["src/components/workstation/WorkScene.tsx", "src/components/workstation/engine.ts", "src/components/workstation/models.ts", "src/components/workstation/workstation.css", "src/App.tsx", "src/components/home/Home.tsx"]
---
Mode: Operate, with Read explanations and the existing crystal holographic scene.
User-pinned direction: TensorSpace-style inspection of real intermediate tensors, continuing the established crystal materials, vertical structures, animated arrows, recursive scene and file-explorer index. No visual-world tournament; FORM seed user-pinned-tensorspace-crystal. No new raster comp or catalog quality card applies.
THESIS: reveal the principal computational skeleton on entry as actual output tensor planes, never generic group boxes or operator counters. Every crystal has a numerical coordinate; statistics unfold inside their owning operation.
OWN-WORLD: cool navy/black operating surface, value-colored cyan positive crystal edges, pale white selected cells, amber negative values. Optional workstation styling in shared crystal primitives preserves homepage and paper defaults. The home remains Silicon Idol S2.
STORY: choose a module, configure dimensions, enter groups and operators in the same canvas, select an output, scrub complete terms, inspect or export all values.
FIRST VIEWPORT: a single principal operation enters its actual arithmetic directly. Compound networks expose principal tensor scaffolds; networks over 100 operations initially focus a named readable stage, with all other stages and Restore full graph retrievable without a global connection web through the focused view. Desktop uses an oblique layer view, a four-root category tree on the left, and parameters on the right. Mobile retains a vertical model view and explicit catalog/parameter disclosures; open parameters precede the viewer.
FORM: user-pinned-tensorspace-crystal. Three-column operating bench; catalog root > task family > module. A stage selector frames a readable portion of the complete execution record; operator indexing is initially collapsed and pages six actual operators at a time. Complex in-scene labels are shown on hover or explicit reveal, while input/output labels remain identifiable. The mathematical tree, KaTeX, full dependencies and paginated tensors remain below. Network topology follows actual dependencies; parallel branches share depth and skip edges run outside the trunk. Last-two-axis tensor planes tile leading coordinates without sampling. Norm/Softmax helper statistics are subordinate; their primary operators are visible at root.
Implementation boundary: 61 executable module definitions including MobileNet V1/V2/V3 complete functional units, adjustable Transformer encoder/decoder stacks, and recurrent networks. Eleven CUT and Diffusion modules cover generator, discriminator, projection, PatchNCE, separate LSGAN goals, forward noise, DDPM/DDIM steps, time embedding, two-scale time-conditioned U-Net and noise-prediction loss. These are configured initialized networks, not trained checkpoint reproductions. IEEE754 float64 browser arithmetic is independently compared to PyTorch 2.9 CPU; GELU uses an erf approximation. No tensor sampling or hidden neurons; folding is structural navigation. Explicit size rejection replaces silent reduction.
Interaction: same-canvas hierarchy, output instance picking, animation/manual progress, native fullscreen, persistent parameters, JSON input validation, full computation export, persistent explanation editing, reduced-motion and WebGL fallback. BatchNorm has initialization-based eval statistics or current-batch statistics; no claim of a full optimizer/training loop.
Preserve all six paper notes and categorized archive navigation. Local build only; no publication authorization.

Reference: TensorSpace src/utils/LayerLocator.js, src/elements/FeatureMap.js and src/layer/intermediate/Conv2d.js inspected from upstream master on 2026-09-14. Adaptation uses current Three.js instancing and actual execution DAG rather than importing the legacy runtime. JSON v2 stores each full tensor once and operator input/output references; it does not eagerly duplicate every per-output trace.

Computation detail: Linear always enters full arithmetic without a group shortcut. Its full weighted connection fabric links every input to all input-weight products, all dot sums, centered bias and output. W is encoded in edge colors and retained in the complete data inspector. Other weighted reductions expose every contribution; max-pooling exposes running comparisons; activation and normalization operations expose full intermediate arrays. Operation-specific abstract diagrams occupy the main inter-layer corridor: convolution scan frames, activation curves, reduction funnels, normalization tapers, gates and reindex braids, with actual operand links. Detached four-cube calculators have been removed. All actual tensor cells are uniform, retain their scalar coordinates, and tile leading planes or long vectors in exact factor rectangles (48 becomes 6 × 8) with no empty corners. Complex labels are lazy hover or explicit reveal; Linear labels use KaTeX and full formulas remain below. Local depth spacing and a longest-dependency vertical trunk prevent oversized tensors elsewhere from stretching every layer. Zoom uses cursor targeting, explicit plus/minus, pan/rotate toggle, two-finger dolly/pan, double-click tensor framing, reset and fullscreen.

Source boundary: CUT uses the no-antialias convolution path; the two-scale time-conditioned U-Net has configured initialized parameters. PatchNCE and LSGAN are independent forward objectives, with no complete trainer or checkpoint. DDPM/DDIM consume externally initialized epsilon predictions. Official implementation links are recorded in generative.ts.

Focused groups keep attached labels for their local input tensors, terminal outputs and the operation driving the live equation. These boundary ports participate in camera fitting; other labels remain lazy. Restore full graph returns to the principal scaffold in the same canvas.
Final visual review: ship; boundary input/output and active-operation labels confirmed on desktop and mobile. Local build only.

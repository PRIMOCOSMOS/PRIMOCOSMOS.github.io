---
version: 1
slug: "src-components-workstation-workstation-tsx"
primary_target: "src/components/workstation/Workstation.tsx"
related_targets: ["src/components/workstation/WorkScene.tsx", "src/components/workstation/engine.ts", "src/components/workstation/execution.ts", "src/components/workstation/operationMotion.ts", "src/components/workstation/activationTransfer.ts", "src/components/workstation/scaffold.ts", "src/components/workstation/models.ts", "src/components/workstation/workstation.css", "src/App.tsx", "src/components/home/Home.tsx"]
---
Mode: Operate, with Read explanations and the existing crystal holographic scene.
User-pinned direction: TensorSpace-style inspection of real intermediate tensors, continuing the established crystal materials, vertical structures, animated arrows, recursive scene and file-explorer index. No visual-world tournament; FORM seed user-pinned-tensorspace-crystal. No new raster comp or catalog quality card applies.
THESIS: preserve fully visible dense input, hidden and output neuron layers with every weight connection. Exact activation transfers and concrete convolution/reduction arithmetic explain the computation in place; all real coordinates and batch identities remain intact.
OWN-WORLD: cool navy/black operating surface, value-colored cyan positive crystal edges, pale white selected cells, amber negative values. Optional workstation styling in shared crystal primitives preserves homepage and paper defaults. The home remains Silicon Idol S2.
STORY: choose a module, configure dimensions, stream the exact execution, examine outputs and terms, then edit explanatory prose or export numerical records.
FIRST VIEWPORT: show the complete small dense network with all baseline-visible weight edges and an upright camera. The MLP/Linear quick parameter rail sits above the canvas and remains available in fullscreen. Long graphs deeper than 24 levels follow the current operator while retaining every stage; full-graph fit, follow toggle and mathematical timeline remain available. Desktop retains the catalog/scene/parameter bench. Mobile keeps catalog and parameter disclosures, with open parameters before the viewer.
FORM: user-pinned-tensorspace-crystal. Three-column operating bench; catalog root > task family > module. A stage selector frames a readable portion of the complete execution record; operator indexing is initially collapsed and pages six actual operators at a time. Complex in-scene labels are shown on hover or explicit reveal, while input/output labels remain identifiable. The mathematical tree, KaTeX, full dependencies and paginated tensors remain below. Network topology follows actual dependencies; parallel branches share depth and skip edges run outside the trunk. Last-two-axis tensor planes tile leading coordinates without sampling. Normalization and stable Softmax statistics are concrete mathematical stages visible in the expanded root graph. Inputs and constants sit near their consumers. MLP/Linear quick parameters expose Batch and input/output features; MLP also exposes hidden channels and hidden layer count. Sidebar controls are retained.
Implementation boundary: 61 executable module definitions including MobileNet V1/V2/V3 complete functional units, adjustable Transformer encoder/decoder stacks, and recurrent networks. Eleven CUT and Diffusion modules cover generator, discriminator, projection, PatchNCE, separate LSGAN goals, forward noise, DDPM/DDIM steps, time embedding, two-scale time-conditioned U-Net and noise-prediction loss. These are configured initialized networks, not trained checkpoint reproductions. IEEE754 float64 browser arithmetic is independently compared to PyTorch 2.9 CPU; GELU uses an erf approximation. No tensor sampling or hidden neurons; folding is structural navigation. Explicit size rejection replaces silent reduction.
Interaction: same-canvas hierarchy, output instance picking, animation/manual progress, native fullscreen, persistent parameters, JSON input validation, full computation export, persistent explanation editing through the top toolbar, independent of live values, reduced-motion and WebGL fallback. BatchNorm has initialization-based eval statistics or current-batch statistics; no claim of a full optimizer/training loop.
Preserve all six paper notes and categorized archive navigation. Local build only; no publication authorization.

Reference: TensorSpace src/utils/LayerLocator.js, src/elements/FeatureMap.js and src/layer/intermediate/Conv2d.js inspected from upstream master on 2026-09-14. Adaptation uses current Three.js instancing and actual execution DAG rather than importing the legacy runtime. JSON v2 stores each full tensor once and operator input/output references; it does not eagerly duplicate every per-output trace.

Computation detail: WorkScene uses executionGraph(..., true), retaining Linear and activation source layers in every scene scope. Each dense W entry maps one-to-one to a visible connection; numerical W remains inspectable and bias docks to output. activationTransfer.ts creates aligned per-coordinate f(x) lanes with exact implemented functions, formulas and moving crystal units, rather than stacks of arithmetic arrays or new prose panels. Gaussian highlight envelopes overlap smoothly across neighboring outputs and terms; all dense edges remain visible at baseline. Vector neurons use uniform .58 crystals and 3.6 horizontal pitch scaling, preserving actual coordinates and batches. Convolution retains streamed exact contributions/partial sums, visible persistent inter-stage links and its main trunk; auxiliaries sit near consumers and zero padding docks to input. Other tensor fields retain uniform .36 cells and exact factor rectangles. The upright camera supports cursor-targeted zoom, plus/minus, pan/rotate, two-finger dolly/pan, double-click tensor framing, reset and fullscreen.

Source boundary: CUT uses the no-antialias convolution path; the two-scale time-conditioned U-Net has configured initialized parameters. PatchNCE and LSGAN are independent forward objectives, with no complete trainer or checkpoint. DDPM/DDIM consume externally initialized epsilon predictions. Official implementation links are recorded in generative.ts.

Focused groups keep attached labels for their local input tensors, terminal outputs and the operation driving the live equation. These boundary ports participate in camera fitting; other labels remain lazy. Restore full graph returns to the complete expanded arithmetic graph in the same canvas.
Recorded evidence: qa/neural-final-*.png records the first rendering QA of this neural-layer revision. Verification covers 61 modules and 124 configurations, all MLP weight counts for Batch 1 and Batch 2, and ten activation transfer functions matching their implemented mathematics. The previous 4,241-stage count describes available expanded execution verification, not the current default visible stage count. Local build only; not deployed.

---
version: 1
slug: "src-components-mmhvaeexplorer-tsx"
primary_target: "src/components/MMHVAEExplorer.tsx"
related_targets: ["src/components/mmhvae/Explorer.tsx","src/components/mmhvae/OrbitScene.tsx","src/components/mmhvae/model.ts","src/components/mmhvae/mmhvae.css"]
---

Mode: Read, with an Experience-led interactive model viewport.
Scope: the MMHVAE paper's existing Method tab only; inherit the dark Deep-Space Telemetry Array identity.
Confirmed direction: orbital model laboratory, selected by the user on 2026-09-05.
Composition: four vertical encoder towers at the perimeter, seven-level shared inference and generation core, four independent image-decoder branches below. Warm neutral PoE surfaces, modality-colored edges, blue shared generation features.
Primary action: follow a module from encoder to Gaussian head, fusion, sample and decoder; inspect source-backed operator order and readable math.
Interaction: rotate, zoom, focus, orthographic presets, isolate one level, spread towers, toggle observed inputs, trace an output, play staged information flow, search every block.
Evidence: commit-pinned official source, local TPAMI paper. Explicitly distinguish paper conventions from actual defaults and inverse-scale fusion implementation.
Accessibility: equivalent HTML module directory, keyboard camera controls, local KaTeX/MathML, reduced motion, on-demand rendering and WebGL fallback.

2026-09-06 follow-up: replace generic block solids with semantic geometry. Click original modules to unfold actual operators over a ghosted overview, recursively inspect SE and output ResNets, and return to the retained overview camera. Layer views expose full conditional generation and observed-expert fusion, with downstream decoders. Major structures label themselves; fine operators reveal names on hover/selection. Tensor annotations and an accessible HTML sequence accompany every scene. Main long-form copy is immediately saved in the browser, with multiline preservation and JSON backup/restore.

User correction: all dissection must be volumetric and in place in the original scene, with real camera motion and retained original outlines. Use horizontal data planes and radial/vertical operator hierarchies. Every internal graph exposes upstream and downstream modules. Separate all central conditional-generation steps, and simplify PoE into a merge disc.

2026-09-06 microscope/index correction: the sole enclosing volume represents the actual selected module; never add a named original-outline object. Internal children and external provenance are separate. Adaptive three-dimensional tiers expand independently at every depth; context sits beyond the boundary, with top/bottom rails on narrow screens. Readable, collision-managed screen-space labels prioritize hovered components. Both canvas and file-explorer navigation follow the same canonical containment path down to atomic operators. Each location consolidates purpose, mathematical principle, tensor information, implementation and children. Keep readable functional names primary and code symbols secondary. Native fullscreen operates on the existing viewer and preserves navigation. Preserve browser-local editable prose. Publication remains outside this local revision.

Latest refinement: each expanded computational block uses a straight vertical trunk, with parallel operations and incoming/outgoing evidence around it. Highest-level structures that are subsets of the overview retain exact overview coordinates, orientation and glyph proportions. Diagram text uses anchored DOM labels with KaTeX/MathML for mathematical symbols and dimensions, fixed screen typography, collision handling and keyboard/click activation. Overview labels explicitly identify four encoders, shared conditional generation, latent scales and input/output groups. Main branch structure takes precedence over compact serpentine packing.

Atomic extension: 2,074 deepest entries across 19 mathematical/data families use same-scene numeric 3D teaching models, not one enlarged glyph. Three or four vertical stages expose the operation; their sources/destinations remain outside. Click a stage or HTML step to focus, then use Complete calculation to restore all stages. Operator-specific probe readouts, manual progress, pause, editable explanation, KaTeX/MathML and full-screen controls remain synchronized. Pure tensor leaves explain organization/indexing; they do not invent hidden computation. Density height scaling is explicitly labeled. Deterministic examples are separate from real model dimensions and trained weights.

2026-09-10 user-pinned material refinement: TensorSpace.js is a conceptual reference for explaining intermediate tensors, not a runtime migration. Tensor elements are equal-sided crystal cubes, with constant size within each tensor, translucent inactive bodies, light-catching faces and fine visible edges. Current coordinates have pale highlights; participating neighborhoods remain colored above the transparent context. Signed values use cold blue/warm amber and numerical readouts, never changing cube height. Convolution patches, dot-product rows, channel statistics, interpolation neighbors and elementwise operands share this vocabulary. Directional arrow trains replace moving balls throughout the overview and detailed scene; both operands point toward a result. Pausing or reduced motion retains static direction cues. Probability surfaces and statistical point clouds retain their mathematical geometry. Refinement preserves the existing navigation, fullscreen viewer, homepage and persistent prose.


2026-09-21 functional composition revision: source functions own contiguous 3D hover regions including inter-layer trunk gaps. Automatic local computation yields to hovered-region playback; leaving resumes the automatic sequence and deeper navigation resets ownership. Only the current complex function allocates its numerical connections. Workstation MLP complete weights and parameters remain. Literature compositions show immediate source children, with real source edges and separate shortcut routing; nested compositions reuse existing numerical visuals lazily. Source tensor extent frames retain exact shape labels and are not sampled activation grids. Numerical atomic teaching examples are explicitly distinct from trained full-shape inference. Static assembly geometry is batched and inactive dynamic objects are disposed. Source catalogs/parameterizations are preserved, including PIGMENT's paper-only boundary. Verification: region/source assembly checks, desktop and mobile hover/click/fullscreen checks, plus existing source/numerical regressions. No deployment.


2026-09-22 paper-viewer correction: source tensor windows replace icon fallbacks. Equal crystal cells carry actual contiguous source indices; known C/H/W are inherited for MMHVAE's channel-only internal annotations, while symbolic inputs remain explicitly unresolved. Coordinate-window paging is available in fullscreen. Static cells are value-neutral source geometry; actual numerical examples are labeled separately and use signed colors/weighted dependency fabrics. Hover shows one active holographic title, without a duplicate DOM title. Expanded views fade only the relevant outer module. Wheel/pinch zoom is enabled and button zoom no longer stops at 2.4. Preserve existing source topology, formulas, lineage, numerical teaching boundaries, and PIGMENT uncertainty.
